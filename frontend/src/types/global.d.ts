type Chat = {
    id: string;
    fullname: string;
    profilePic: string;
    // lastMessage: string;
    // lastMessageTime: string;
    // unread: number;
};

type Message = {
    id: string;
    senderId: string;
    text: string;
    time: string;
};
